Vectors or $n$-tuples can be used as a way to work with primes/factors.

We can entirely represent the number $A$ by its exponent (or something else) vector (or $n$-tuple):

$$ A \longrightarrow (x_1, x_2, \dots, x_n)$$
$$